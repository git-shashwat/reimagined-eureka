import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    marginVertical: 10,
  },
  input: {
    fontSize: 48,
    textAlign: "center",
    paddingVertical: 10,
  },
});

export default styles;
